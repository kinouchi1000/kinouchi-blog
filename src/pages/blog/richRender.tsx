import { documentToReactComponents } from '@contentful/rich-text-react-renderer'
import React, { FC ,ReactNode} from 'react'

export const richRender:React.FC =({ body }: { children?: ReactNode; body?: string })=>{
  return(
    <>
      
    </>
  )
}